library ui_style;

import 'package:flutter/material.dart';

const Color primaryColor = Colors.red;
const Color lightTextPrimaryColor = Colors.white;
const Color darkTextPrimaryColor = Colors.black;

BorderRadius defaultBorderRadius = BorderRadius.circular(12);

const TextStyle characterID = TextStyle(color: Colors.grey, fontSize: 16);
const TextStyle characterTitle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: 22);
const TextStyle characterDescription = TextStyle(fontSize: 16);
const TextStyle characterListItemSectionTitle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: 18);
const TextStyle characterListItemName = TextStyle(fontSize: 16);
