import 'package:flutter/material.dart';

class SearchSugestionsWidget extends StatelessWidget {
  const SearchSugestionsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}